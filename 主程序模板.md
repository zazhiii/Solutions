```java

import java.io.*;
import java.util.*;
public class Main {
	static Read rd = new Read();
	static Scanner sc = new Scanner(System.in);
	static PrintWriter pw = new PrintWriter(new BufferedWriter(new OutputStreamWriter(System.out)));
	static long INF = (long)2e18;static int inf = (int)2e9;
	//---------
	public static void main(String[] args) throws IOException{
		int T = 1;
//		T = rd.nextInt();
		while(T -- > 0) solve();
		pw.flush();
		pw.close();
	}
	

	private static void solve() throws IOException {
		// **这里写逻辑**
	}
	
	
	static void iarr(int a[], int s, int e) throws IOException {//读数组
		for(int i = s; i <= e; i++) a[i] = rd.nextInt();
	}
	static void oarr(int a[], int s, int e) throws IOException {//输出数组
		for(int i = s; i<= e; i++) pw.print(a[i] + " ");
		pw.println();
	}
	static void oarrln(int a[], int s, int e) throws IOException {//输出数组+\n
		for(int i = s; i<= e; i++) pw.println(a[i]);
	}
}

class Read{//快读
	StreamTokenizer st = new StreamTokenizer(new BufferedReader(new InputStreamReader(System.in)));
	public int nextInt() throws IOException{
		st.nextToken();
		return (int)st.nval;
	}
	public long nextLong()throws Exception{
		st.nextToken();
		return (long)st.nval;
	}
	public double nextDouble()throws Exception{
		st.nextToken();
		return (long)st.nval;
	}
}
```

